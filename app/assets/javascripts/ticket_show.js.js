// Generated by CoffeeScript 1.7.1
(function() {
  var cal_booking_fee;

  cal_booking_fee = function(quantity) {
    var _ref;
    return (_ref = quantity > 50) != null ? _ref : {
      10: 5
    };
  };

  $(function() {
    return $('#order_count').change(function() {
      var booking_fee, flat_price, quantity, total;
      flat_price = $('#flat-price').text().trim;
      flat_price = parseInt(flat_price);
      quantity = $('#order_count').val();
      total = flat_price * quantity;
      $('#total-price').val(total);
      $('#order_price').val(total);
      booking_fee = cal_booking_fee(quantity);
      $('#booking-fee').val('$' + booking_fee);
      return $('#order_booking_fee').val(booking_fee);
    });
  });

}).call(this);
